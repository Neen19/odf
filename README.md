Для запуска программы необходимо создать venv и скачать зависимости

python3 -m venv venv

source venv/bin/activate

pip install --upgrade pip

pip install pyminizip

в случае ошибок - аналогично добавить недостающие зависимости
